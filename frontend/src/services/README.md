# Services Directory

Backend API client wrappers matching `API_CONTRACT.md`:

- `api.js`: Axios / Fetch wrapper functions for fetching ticket lists, ticket details, triggering ticket evaluation, and fetching decision logs.
