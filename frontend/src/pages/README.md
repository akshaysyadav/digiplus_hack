# Pages Directory

Primary page views for the React frontend:

- `Dashboard.jsx`: Two-lane dashboard view (Auto-Resolved Lane & Needs-Human Lane).
- `TicketDetailView.jsx`: In-depth inspection view for a selected ticket.
