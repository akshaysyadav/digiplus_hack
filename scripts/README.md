# Scripts Directory — Zepto Support Ticket Manager

Utility and setup scripts directory:

- `validate_data.py`: (Planned) Verifies integrity of CSV files in `data/`.
- `run_evaluation_batch.py`: (Planned) Batch runs incoming tickets through the decision engine for offline testing.
